{
  "package": "napi-rust-test",
  "devDependencies": {
    "@napi-rs/cli": "^1.0.0"
  },
  "napi": {
    "name": "calc-lib"
  },
  "scripts": {
    "build": "napi build --release",
    "build:debug": "napi build"
  }
}
